{
  "scripts": {
    "prestart": "gatsby clean",
    "start": "gatsby develop",
    "build": "gatsby build",
    "serve": "gatsby serve",
    "pack": "[ ! -d dist ] && mkdir dist || /bin/echo -n; tar czpf dist/codestream-guide.tgz public",
    "deploy": "rsync -av --delete public web@tc:/cs/mnt/local/guide"
  },
  "dependencies": {
    "gatsby": "^2.20.20",
    "gatsby-theme-apollo-docs": "^4.1.4",
    "react": "^16.13.1",
    "react-dom": "^16.13.1"
  }
}
